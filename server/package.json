{
  "name": "lingua-image-proxy",
  "version": "1.0.0",
  "description": "Image-generation proxy for the Lingua app. Keeps the OpenAI key server-side and returns raw PNG bytes.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "gemini": "node gemini_proxy_server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "openai": "^4.77.0"
  }
}
